{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "pnpm run routes:generate && pnpm run check:coverage && orval --config ./orval.config.ts && pnpm -w run typecheck:libs",
    "routes:generate": "node ./scripts/generate-route-inventory.mjs",
    "check:coverage": "node ./scripts/check-route-coverage.mjs"
  },
  "devDependencies": {
    "orval": "^8.5.2"
  }
}
